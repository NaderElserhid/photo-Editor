import React from 'react';
import { IState } from '../../models/index.models';
import './EditImage.scss';
export interface EditImageProps {
    labels: any;
    image: string | null | undefined;
    color: string;
    initialState: IState;
    saveUpdates: Function;
    rtl: boolean;
    dark: boolean;
}
declare const EditImage: React.MemoExoticComponent<({ labels, image, color, initialState, rtl, dark, saveUpdates, }: EditImageProps) => React.JSX.Element>;
export default EditImage;
